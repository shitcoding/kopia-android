# E2E Test: FileBrowser Select All and Deselect
# Tests Select All button selects entire visible directory, Deselect clears
# Repository: /sdcard/testrepo
# Password: test123
#
# Navigation: ConnectScreen → SnapshotsScreen → SourceSnapshotsScreen → FileBrowserScreen
#
# UI is rendered in WebView (React app).

appId: org.kopiaKt.app
androidWebViewHierarchy: devtools
---
# Clear app state and start fresh
- stopApp
- launchApp

# Connect to repository
- extendedWaitUntil:
    visible: "KopiaKt"
    timeout: 10000
- tapOn: "Connect to Repository"

- extendedWaitUntil:
    visible: "Connect Repository"
    timeout: 5000

- tapOn:
    id: "repo-path-input"
- inputText: "/sdcard/testrepo"

- tapOn:
    id: "repo-password-input"
- inputText: "test123"

- hideKeyboard
- tapOn:
    id: "connect-button"

# SnapshotsScreen → SourceSnapshotsScreen
- extendedWaitUntil:
    visible:
      text: ".*76 files.*"
    timeout: 15000
- tapOn:
    text: ".*76 files.*"

# SourceSnapshotsScreen → FileBrowserScreen
- extendedWaitUntil:
    visible:
      text: ".* snapshots"
    timeout: 10000
- extendedWaitUntil:
    visible:
      text: ".*76.*files.*"
    timeout: 10000
- tapOn:
    text: ".*76.*files.*"

# FileBrowserScreen root: wait for file list
- extendedWaitUntil:
    visible: "/"
    timeout: 15000
- extendedWaitUntil:
    visible:
      text: ".*level1.*"
    timeout: 10000

# Enter selection mode by long-pressing on the hidden_dir row
- longPressOn:
    text: "Folder hidden_dir"

# Wait for selection toolbar (text may be split across lines in WebView)
- extendedWaitUntil:
    visible:
      text: ".*selected.*"
    timeout: 10000

- assertVisible:
    text: ".*selected.*"
- assertVisible: "Select all"

# Tap Select All
- tapOn: "Select all"

# Verify "Deselect" appears (meaning all items selected, same button toggles)
- extendedWaitUntil:
    visible: "Deselect all"
    timeout: 3000

- assertVisible: "Deselect all"

# Verify selected count is greater than 1
- assertVisible:
    text: ".*selected.*"

# Tap Deselect to clear all
- tapOn: "Deselect all"

# Wait for "Select All" to reappear (Deselect toggled back)
# and count should be 0 or the toolbar should update
- extendedWaitUntil:
    visible: "Select all"
    timeout: 3000

# Tap X to exit selection mode
- tapOn: "Clear selection"

# Verify normal file list icons are restored (no checkboxes)
# and selection toolbar is gone
- extendedWaitUntil:
    notVisible: "selected"
    timeout: 3000
